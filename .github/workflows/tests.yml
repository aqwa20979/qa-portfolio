name: Run Tests

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'README.md'

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_PASSWORD: mysecretpassword
          POSTGRES_DB: shop
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.14'

    - name: Install dependencies
      run: |
        pip install pytest requests playwright pytest-playwright psycopg2-binary allure-pytest
        pip uninstall -y pytest-base-url || true
        playwright install chromium

    - name: Run tests with Allure
      env:
        POSTGRES_HOST: postgres
      run: |
        pytest api_tests/ -v --alluredir=allure-results

    - name: Install Allure
      run: |
        wget https://github.com/allure-framework/allure2/releases/download/2.33.0/allure-2.33.0.tgz
        tar -xzf allure-2.33.0.tgz
        sudo mv allure-2.33.0 /opt/allure
        sudo ln -s /opt/allure/bin/allure /usr/local/bin/allure

    - name: Generate Allure report
      run: |
        allure generate allure-results --clean -o allure-report

    - name: Deploy Allure report to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./allure-report
        publish_branch: gh-pages
        keep_files: true